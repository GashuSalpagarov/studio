"use client";

import type { ReactNode } from "react";

// TODO: Lenis provider + GSAP ticker bridge — см. docs/02-architecture.md
export function Providers({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
